# 图像标注服务平台 MVP 需求文档

## 1. 一句话产品定义

一个面向任务需求方与标注承接方的图像标注协作平台，支持需求方发布图像标注任务，标注方试标报价，需求方选择报价后进入正式标注，并完成逐条抽检、驳回申诉、数据回收和统计结算。

## 2. 产品目标

### 2.1 MVP 目标

在不接入数据库和真实后端的前提下，使用 `Next.js + TypeScript + localStorage` 搭建一个可演示、可体验、流程完整的图像标注任务平台原型。

### 2.2 核心价值

- 需求方可以发布图像标注任务，并通过试标报价选择合适的标注方
- 标注方可以参与试标、提交报价、承接正式标注任务
- 双方可以围绕正式标注结果完成子任务包提交、抽检验收、驳回、申诉、中止和数据下载

## 3. 目标用户

### 3.1 需求方

需求方通常是有图像数据标注需求的业务方、算法团队、产品团队或数据团队。

核心诉求：

- 创建并发布图像标注任务
- 从 0 创建任务，或基于历史任务创建新任务
- 配置图片展示方式和标签规则
- 指定标注方或开放任务给多个标注方试标报价
- 查看试标结果和报价，选择正式标注方
- 跟踪正式标注任务包进度
- 创建抽检批次，通过随机抽检、按标签筛选抽检或手动选择抽检来验收任务包
- 查看标注方的申诉或质疑
- 中止任务，并下载符合规则的已完成标注数据

### 3.2 标注方

标注方通常是承接图像标注任务的供应商、团队或个人标注员。

核心诉求：

- 浏览可参与试标的任务
- 查看任务规则、图片展示方式和标签配置
- 完成试标并提交报价
- 被选择后完成正式标注
- 设置正式标注子任务包大小
- 按子任务包完成正式标注和提交
- 根据抽检驳回原因修改整个子任务包或提出一次申诉
- 查看任务统计与预计结算

## 4. 核心功能与优先级

MVP 版本核心功能不超过 3 个。

### P0：任务创建、试标与报价

需求方可以从 0 创建图像标注任务，也可以基于已完成历史任务创建新任务；任务可保存草稿或发布。发布后可以进入试标报价流程，也可以在指定标注方和手动填写单价后直接进入正式标注。

包含能力：

- 需求方创建图像标注任务
- 支持从 0 创建任务
- 支持基于已完成历史任务创建任务，默认填充历史任务参数，但允许修改
- 保存草稿，草稿不进入任务大厅，也不能被试标或报价
- 通过图片 URL 批量录入图像数据
- 配置试标数据和正式标注数据
- 配置图片展示形式
- 配置标签标题、选项、单选或多选、判断依据
- 指定标注方或开放给多个标注方参与试标报价
- 从 0 创建并直接进入正式标注时，必须指定标注方并手动填写每条数据单价
- 标注方完成试标并提交报价
- 需求方查看试标结果和报价，选择一家标注方进入正式标注

### P0：正式标注与提交

被选中的标注方可以进入正式标注工作台，设置每包数据条数，系统按正式数据顺序自动生成子任务包；标注方按子任务包完成图像标注并提交。

包含能力：

- 查看正式任务详情
- 查看标注规则、培训说明、图片展示配置和标签配置
- 设置子任务包大小
- 对正式标注数据按包逐条标注
- 保存标注进度
- 提交子任务包标注结果
- 被驳回后修改并重新提交整个子任务包

### P1：抽检验收、驳回申诉、中止与统计

需求方可以针对正式标注子任务包创建抽检批次，通过随机抽检、按标签筛选抽检或手动选择抽检来决定任务包是否通过；标注方可以针对驳回原因修改整个包，或提出一次申诉/质疑；双方可以查看任务数、任务包数和数据条数统计。

包含能力：

- 需求方查看任务进度和正式标注提交结果
- 针对子任务包创建抽检批次
- 支持随机抽检、按标签筛选抽检、手动选择抽检
- 针对抽检数据执行通过或驳回
- 驳回时填写问题类型和单条数据的驳回原因
- 标注方查看抽检驳回原因
- 标注方修改被驳回的整个子任务包并重新提交
- 标注方针对驳回原因提出一次申诉/质疑
- 需求方查看并处理申诉/质疑
- 需求方可以中止任务并填写中止原因
- 任务完成或中止后按规则下载 JSON 标注结果
- 双方查看基础统计和预计结算

基础统计包括：

- 总任务数
- 待试标任务数
- 正式标注中任务数
- 待验收任务数
- 已完成任务数
- 已中止任务数
- 子任务包总数
- 待验收子任务包数
- 通过子任务包数
- 部分驳回子任务包数
- 总数据条数
- 已标注数据条数
- 待验收数据条数
- 通过数据条数
- 驳回数据条数
- 申诉/质疑条数
- 预计结算金额

## 5. 用户流程

### 5.1 需求方流程

从打开平台到完成数据回收：

1. 需求方打开平台首页
2. 选择身份为「需求方」
3. 进入需求方工作台
4. 点击「发布任务」
5. 选择创建方式：从 0 创建，或基于已完成历史任务创建
6. 如果基于历史任务创建，系统默认填充历史任务参数，需求方可以修改
7. 填写任务基础信息：任务名称、任务描述、截止时间、培训说明、标注规则
8. 批量录入图片 URL，每行代表一条图像数据；如一条数据包含多张图片，则在同一行录入一组图片 URL
9. 配置图片展示形式：单张或多张、并列展示或单张切换、是否允许双击放大、放大后的展示形式
10. 配置多个标签问题组；每个问题组包含标题、单选或多选、标签选项、每个选项的判断依据
11. 选择任务进入方式：试标报价后进入正式标注，或直接进入正式标注
12. 如果选择试标报价，配置试标数据抽取方式：前 N 条作为试标，或随机抽取 N 条作为试标
13. 如果选择直接进入正式标注，必须指定标注方；从 0 创建时还必须手动填写每条数据单价
14. 点击「保存草稿」时，任务进入「草稿」状态，不进入任务大厅
15. 点击「发布任务」后，根据任务进入方式进入试标阶段或正式标注阶段
16. 如果进入试标阶段且已指定标注方，仅指定标注方可试标；如果未指定标注方，多个标注方可以参与试标并提交报价
17. 需求方在工作台查看报价对比表，包含标注方、报价单价、报价说明、提交时间和选择按钮
18. 需求方选择一家标注方作为正式标注方，系统记录该报价为正式结算单价，其他报价变为未中标且不可修改
19. 正式标注方按子任务包提交标注结果
20. 需求方针对某个子任务包创建抽检批次
21. 需求方通过随机抽检、按标签筛选抽检或手动选择抽检确定本次抽检数据
22. 需求方对抽检数据点击「通过」或「驳回」
23. 任一抽检数据被驳回时，该子任务包自动进入「部分驳回」状态
24. 标注方修改后重新提交整个子任务包；重新提交后整个子任务包回到「待验收」
25. 如果标注方对驳回原因有异议，可以提交一次申诉/质疑
26. 需求方查看申诉/质疑，并选择维持驳回或改为通过，可追加处理说明
27. 子任务包没有被驳回数据时，需求方可以点击「通过整个包」；包内未抽检数据也视为通过
28. 所有正式子任务包通过后，任务进入「已完成」
29. 需求方可以下载任务标注结果
30. 需求方也可以在任务未完成时中止任务，中止时需要填写中止原因

### 5.2 标注方流程

从打开平台到完成正式标注结算：

1. 标注方打开平台首页
2. 选择身份为「标注方」
3. 进入标注方任务大厅
4. 浏览可参与试标的任务
5. 查看任务详情：任务说明、培训说明、标注规则、图片展示配置、标签配置、试标数据量、正式数据量
6. 如果任务未指定标注方，点击「参与试标」
7. 如果任务已指定给当前标注方，直接进入试标工作台
8. 在试标工作台完成少量图像数据标注
9. 提交试标结果、报价单价和报价说明
10. 等待需求方选择报价
11. 如果未被选择，任务结束，不产生结算
12. 如果被选择，进入正式标注阶段
13. 首次进入正式标注工作台时，设置每包数据条数
14. 系统按正式数据顺序自动生成子任务包；最后一个包可以少于每包数据条数
15. 标注方选择一个子任务包进入包内逐条标注
16. 标注过程中可以保存进度
17. 完成一个子任务包后，点击「提交子任务包」
18. 等待需求方针对该包创建抽检批次并验收
19. 对通过的任务包查看通过状态
20. 对部分驳回的任务包查看问题类型和驳回原因
21. 对确实需要修改的任务包进行修改并重新提交整个包
22. 对有异议的驳回原因提交一次申诉/质疑
23. 所有正式标注数据通过验收后，查看预计结算金额

## 6. 页面结构

### 6.1 首页

路径建议：`/`

页面目标：让用户选择身份并进入对应工作区。

页面内容：

- 平台名称
- 一句话产品介绍
- 身份选择入口：我是需求方、我是标注方
- 最近任务概览，可选

核心操作：

- 进入需求方工作台
- 进入标注方任务大厅

### 6.2 需求方工作台

路径建议：`/requester/dashboard`

页面目标：合并需求方控制台与任务详情页，让需求方在一个页面中完成任务管理、报价选择、进度跟踪、抽检验收和数据下载。

页面布局建议：

- 左侧或上方展示任务列表
- 右侧或下方展示选中任务详情

页面内容：

- 数据概览：任务数、正式标注中任务数、待验收任务数、已完成任务数、总数据条数、已标注数据条数、通过数据条数、驳回数据条数
- 任务列表：任务名称、阶段、状态、标注方、报价状态、任务包进度、正式数据进度、截止时间
- 选中任务基础信息：任务说明、培训说明、图片展示配置、标签配置
- 试标与报价区域：报价对比表，展示标注方、报价单价、报价说明、提交时间、选择报价按钮；MVP 不展示试标标注结果预览
- 正式标注进度区域：子任务包总数、待验收包数、通过包数、部分驳回包数、总数据条数、已标注条数、通过条数、驳回条数
- 子任务包区域：包编号、包状态、数据条数、已标注条数、抽检批次数、通过/驳回操作入口
- 抽检批次区域：批次 ID、抽检方式、抽检数据、通过数量、驳回数量、创建时间、操作人
- 抽检验收区域：图片、标注结果、当前验收状态、通过按钮、驳回按钮、问题类型、驳回原因
- 申诉/质疑区域：被驳回数据、标注方说明、处理入口
- 数据下载区域：任务完成或中止后下载 JSON 标注结果
- 中止任务区域：中止按钮、中止原因输入、已通过数据结算金额

核心操作：

- 发布新任务
- 保存或编辑草稿
- 编辑已发布但未进入正式标注的任务配置
- 查看试标结果
- 选择报价并确定正式标注方
- 查看正式标注进度
- 创建抽检批次
- 通过整个子任务包
- 驳回抽检数据并使包进入部分驳回
- 处理申诉/质疑
- 中止任务
- 下载标注结果

### 6.3 发布任务页

路径建议：`/requester/tasks/new`

页面目标：让需求方创建一个新的图像标注任务，并配置试标、正式标注、图片展示和标签规则。

页面字段：

- 创建方式：从 0 创建、基于历史任务创建
- 历史任务，选择基于历史任务创建时必填，只展示已完成任务
- 任务进入方式：试标报价后进入正式标注、直接进入正式标注
- 任务名称
- 任务描述
- 培训说明
- 标注规则
- 截止时间
- 是否指定标注方
- 指定标注方，非必填
- 手动单价，选择直接进入正式标注且从 0 创建时必填
- 图片 URL 数据
- 试标抽取方式：前 N 条、随机抽取 N 条
- 试标数据条数
- 正式标注数据范围或正式数据条数
- 图片展示配置
- 标签配置
- 备注

MVP 图片数据录入方式：

- 使用文本框批量输入图片 URL
- 每行代表一条待标注数据
- 单条数据只有一张图片时，每行输入一个 URL
- 单条数据包含多张图片时，每行输入多个 URL，可使用逗号分隔

示例：

```text
https://example.com/image-001.jpg
https://example.com/image-002-a.jpg, https://example.com/image-002-b.jpg
https://example.com/image-003.jpg
```

图片展示配置：

- 数据形态：单张图片、多张图片
- 多张图片展示方式：全部并列展示、单张展示并支持左右切换
- 是否支持双击放大：是、否
- 放大后展示形式：弹窗居中展示、全屏沉浸展示

标签配置：

- 支持多个标签问题组
- 每个问题组包含标签标题，例如「图片是否包含违规内容」
- 每个问题组独立配置选择方式：单选、多选
- 每个问题组配置标签选项，例如「是」「否」「无法判断」
- 每个选项填写判断依据
- 每条数据必须完成所有问题组后才算已标注
- AI 审核工具说明：后台未来可以基于标签选项和判断依据校验选项之间是否存在语义重叠、判断依据是否清晰；MVP 不实现真实后台 AI 审核能力

核心操作：

- 保存草稿
- 发布任务
- 基于历史任务填充配置
- 编辑已发布但未进入正式标注的任务配置

发布规则：

- 保存草稿后，任务状态为「草稿」
- 草稿不进入标注方任务大厅
- 发布任务后，任务根据进入方式进入试标阶段或正式标注阶段
- 选择试标报价流程时，发布任务时不填写结算单价，结算单价由标注方试标报价后产生，并由需求方选择报价后确定
- 如果选择直接进入正式标注，则必须指定标注方
- 如果从 0 创建且直接进入正式标注，则必须手动填写每条数据单价
- 如果基于历史任务创建，历史任务参数默认填充但都可以修改
- 已发布但未进入正式标注前允许编辑配置；编辑后已提交报价不会自动作废，需求方需自行判断报价是否仍有效

### 6.4 标注方任务大厅

路径建议：`/annotator/tasks`

页面目标：让标注方浏览可试标任务、进入试标工作台或查看自己承接的正式任务。

页面内容：

- 可试标任务列表
- 已参与试标任务列表
- 已中标正式任务列表
- 筛选条件：任务阶段、任务状态、是否已报价、是否指定给我
- 每个任务展示：任务名称、阶段、状态、试标数据条数、正式数据条数、截止时间、是否指定、报价状态
- 指定给其他标注方的任务对当前标注方隐藏

核心操作：

- 查看任务详情
- 参与试标
- 进入试标工作台
- 进入正式标注工作台

### 6.5 标注方任务详情页

路径建议：`/annotator/tasks/[taskId]`

页面目标：让标注方在试标或正式标注前了解任务要求。

页面内容：

- 任务说明
- 培训说明
- 标注规则
- 图片展示配置
- 标签配置
- 试标数据条数
- 正式数据条数
- 当前阶段和状态
- 是否指定标注方
- 当前报价状态
- 子任务包状态，正式阶段展示

核心操作：

- 参与试标
- 进入试标工作台
- 进入正式标注工作台

### 6.6 试标工作台

路径建议：`/annotator/tasks/[taskId]/trial`

页面目标：让标注方完成少量试标数据，并提交报价。

页面内容：

- 当前任务信息
- 培训说明
- 标注规则
- 图片展示区域
- 标签选择区域
- 试标进度
- 报价单价输入
- 报价说明输入

核心操作：

- 上一条
- 下一条
- 保存试标进度
- 提交试标和报价

试标规则：

- 试标数据不参与结算
- 试标必须完成后才能提交报价
- 报价提交后，需求方可查看试标结果和报价说明
- 未被选择的标注方不会进入正式标注阶段

### 6.7 正式标注工作台

路径建议：`/annotator/tasks/[taskId]/workspace`

页面目标：让被选择的标注方完成正式图像标注。

页面内容：

- 当前任务信息
- 培训说明
- 标注规则
- 图片展示区域
- 标签选择区域
- 正式标注进度
- 子任务包列表
- 已驳回数据列表
- 申诉/质疑入口

子任务包行为：

- 标注方首次进入正式标注工作台时设置每包数据条数
- 系统按正式数据顺序自动生成子任务包
- 如果最后剩余数据少于每包数据条数，最后一个包包含剩余全部数据
- 标注方可以修改每包数据条数
- 修改包大小只影响未提交、未验收的后续包
- 已提交、待验收、已通过或部分驳回的包不重切
- 标注方以「包列表 -> 包内逐条标注」形式完成正式标注
- 子任务包必须作为整体提交
- 部分驳回的子任务包重新提交时，需要重新提交整个包
- 重新提交后整个子任务包回到「待验收」，并保留历史抽检和驳回记录

图片展示行为：

- 单张图片任务：每条数据展示一张图片
- 多张图片并列展示：同一条数据下的多张图片在窗口内全部并列展示
- 多张图片切换展示：同一条数据下只展示一张图片，支持左右切换
- 如果允许双击放大，双击图片后按配置使用弹窗居中展示或全屏沉浸展示

标签操作行为：

- 单选问题组：每条数据在该问题组下只能选择一个标签选项
- 多选问题组：每条数据在该问题组下可以选择多个标签选项
- 标注方可以查看每个选项的判断依据
- 每条数据必须完成所有问题组后才算已标注

核心操作：

- 上一条
- 下一条
- 保存进度
- 设置或修改每包数据条数
- 提交子任务包
- 修改被驳回的子任务包
- 针对驳回原因提交申诉/质疑

### 6.8 标注方统计页

路径建议：`/annotator/stats`

页面目标：让标注方查看自己的任务完成情况、数据条数和预计结算。

页面内容：

- 已参与试标任务数
- 已报价任务数
- 已中标任务数
- 正式标注中任务数
- 已完成任务数
- 已中止任务数
- 子任务包总数
- 已提交子任务包数
- 已通过子任务包数
- 部分驳回子任务包数
- 正式标注总数据条数
- 已标注数据条数
- 通过数据条数
- 驳回数据条数
- 申诉/质疑条数
- 预计结算金额
- 任务明细列表

## 7. 数据结构设计

由于 MVP 暂不使用数据库，所有数据存储在 `localStorage` 中。

### 7.1 Task 数据结构

```ts
type TaskStatus =
  | "draft"
  | "pending_trial"
  | "trial_in_progress"
  | "pending_quote_selection"
  | "formal_in_progress"
  | "pending_review"
  | "partially_rejected"
  | "completed"
  | "cancelled";

type TaskStage = "draft" | "trial" | "formal" | "review" | "completed" | "cancelled";

type AnnotationType = "image_annotation";

type TaskCreationMode = "from_scratch" | "from_history";
type TaskEntryMode = "trial_quote" | "direct_formal";
type TrialSamplingMode = "first_n" | "random_n";

type Task = {
  id: string;
  title: string;
  description: string;
  annotationType: AnnotationType;
  creationMode: TaskCreationMode;
  historyTaskId?: string;
  entryMode: TaskEntryMode;
  trialSamplingMode?: TrialSamplingMode;
  trialSampleSize?: number;
  rules: string;
  trainingContent: string;
  stage: TaskStage;
  status: TaskStatus;
  requesterId: string;
  assignedAnnotatorId?: string;
  selectedAnnotatorId?: string;
  quotedUnitPrice?: number;
  manualUnitPrice?: number;
  deadline: string;
  displayConfig: DisplayConfig;
  labelConfigs: LabelConfig[];
  trialItems: AnnotationItem[];
  formalItems: AnnotationItem[];
  packages: AnnotationPackage[];
  reviewBatches: ReviewBatch[];
  quotes: Quote[];
  cancelReason?: string;
  cancelledAt?: string;
  createdAt: string;
  updatedAt: string;
};
```

### 7.2 AnnotationItem 数据结构

```ts
type AnnotationReviewStatus =
  | "not_submitted"
  | "pending_review"
  | "approved"
  | "rejected"
  | "appealed";

type RejectionIssueType =
  | "label_error"
  | "image_unclear"
  | "rule_misunderstanding"
  | "missing_annotation"
  | "other";

type AnnotationItem = {
  id: string;
  imageUrls: string[];
  packageId?: string;
  annotationValues?: AnnotationValue[];
  annotationNote?: string;
  reviewStatus: AnnotationReviewStatus;
  rejectionIssueType?: RejectionIssueType;
  rejectionReason?: string;
  appealReason?: string;
  appealHandledReason?: string;
  hasAppealed?: boolean;
  updatedAt: string;
};
```

### 7.3 AnnotationValue 数据结构

```ts
type AnnotationValue = {
  labelConfigId: string;
  value: string | string[];
};
```

### 7.4 AnnotationPackage 数据结构

```ts
type PackageStatus =
  | "not_started"
  | "in_progress"
  | "pending_review"
  | "approved"
  | "partially_rejected";

type AnnotationPackage = {
  id: string;
  taskId: string;
  annotatorId: string;
  itemIds: string[];
  status: PackageStatus;
  submittedAt?: string;
  approvedAt?: string;
  updatedAt: string;
};
```

### 7.5 ReviewBatch 数据结构

```ts
type ReviewSamplingMode = "random" | "label_filter" | "manual";

type ReviewBatch = {
  id: string;
  taskId: string;
  packageId: string;
  samplingMode: ReviewSamplingMode;
  sampledItemIds: string[];
  approvedCount: number;
  rejectedCount: number;
  operatorId: string;
  createdAt: string;
};
```

### 7.6 Quote 数据结构

```ts
type QuoteStatus = "draft" | "submitted" | "selected" | "not_selected";

type Quote = {
  id: string;
  taskId: string;
  annotatorId: string;
  trialItemIds: string[];
  unitPrice: number;
  quoteNote: string;
  status: QuoteStatus;
  submittedAt: string;
};
```

### 7.7 DisplayConfig 数据结构

```ts
type ImageDataMode = "single_image" | "multi_image";
type MultiImageDisplayMode = "parallel" | "carousel";
type ZoomDisplayMode = "modal" | "fullscreen";

type DisplayConfig = {
  imageDataMode: ImageDataMode;
  multiImageDisplayMode?: MultiImageDisplayMode;
  enableDoubleClickZoom: boolean;
  zoomDisplayMode?: ZoomDisplayMode;
};
```

### 7.8 LabelConfig 数据结构

```ts
type LabelSelectionMode = "single" | "multiple";

type LabelOption = {
  id: string;
  label: string;
  criteria: string;
};

type LabelConfig = {
  id: string;
  title: string;
  selectionMode: LabelSelectionMode;
  options: LabelOption[];
};
```

### 7.9 DemoUser 数据结构

```ts
type DemoUser = {
  id: string;
  role: UserRole;
  name: string;
};
```

### 7.10 UserRole 数据结构

```ts
type UserRole = "requester" | "annotator";
```

## 8. 状态流转

```text
草稿
 ↓ 发布
待试标
 ↓ 标注方开始试标
试标中
 ↓ 标注方提交试标和报价
待选择报价
 ↓ 需求方选择报价
正式标注中
 ↓ 标注方提交正式标注
待验收
 ↓              ↘
已完成          部分驳回
                  ↓ 修改或申诉
                待验收

任意未完成状态
 ↓ 需求方中止
已中止
```

状态说明：

| 状态 | 含义 |
| --- | --- |
| 草稿 | 需求方创建但未发布，不进入任务大厅 |
| 待试标 | 任务已发布，等待标注方参与试标；若已指定标注方，仅指定方可试标 |
| 试标中 | 标注方正在完成试标数据 |
| 待选择报价 | 至少一个标注方已提交试标和报价，等待需求方选择 |
| 正式标注中 | 需求方已选择报价，正式标注方正在标注正式数据 |
| 待验收 | 正式标注方已提交子任务包，等待需求方创建抽检批次并验收 |
| 部分驳回 | 存在被驳回的数据，标注方需要修改或申诉 |
| 已完成 | 所有正式标注数据均已通过验收 |
| 已中止 | 需求方主动中止任务，任务不可继续标注、报价、提交或申诉 |

阶段说明：

| 阶段 | 含义 |
| --- | --- |
| 草稿阶段 | 任务未发布，可编辑保存 |
| 试标阶段 | 标注方完成少量数据并报价，不参与结算 |
| 正式标注阶段 | 被选择的标注方完成正式数据标注 |
| 验收阶段 | 需求方逐条通过或驳回正式标注数据 |
| 完成阶段 | 全部正式数据通过，可回收和计算预计结算 |
| 中止阶段 | 任务被需求方中止，仅保留历史数据和已通过数据结算 |

## 9. MVP 交互规则

### 9.1 身份规则

MVP 不做真实登录。

用户进入平台后，通过选择身份切换视角：

- 需求方视角
- 标注方视角

身份信息存储在 `localStorage`。

### 9.2 草稿规则

- 需求方创建任务时可以保存草稿
- 草稿保留任务基础信息、图片 URL、展示配置和标签配置
- 草稿不进入标注方任务大厅
- 草稿不能被试标、报价或领取
- 草稿发布后进入试标阶段

### 9.3 试标与报价规则

- 发布任务时不填写结算单价
- 标注方必须先完成试标数据，才能提交报价
- 报价包含报价单价和报价说明
- 试标数据不参与结算
- 未指定标注方时，多个标注方可以参与试标报价
- 指定标注方时，任务自动分配给指定标注方，其他标注方不可参与
- 需求方选择报价后，报价单价成为正式结算单价
- 未被选择的标注方不进入正式标注阶段
- 需求方工作台需要提供报价对比表
- 报价对比表展示标注方、报价单价、报价说明、提交时间和选择按钮
- 需求方选择报价后，其他报价变为未中标且不可修改
- 报价对比表不展示试标标注结果预览
- 已提交报价不会因为需求方编辑任务配置自动作废

### 9.4 直接进入正式标注规则

- 需求方创建任务时可以选择直接进入正式标注
- 从 0 创建并直接进入正式标注时，必须指定标注方
- 从 0 创建并直接进入正式标注时，需求方必须手动填写每条数据单价，该单价作为正式结算单价
- 基于历史任务创建时，也可以选择直接进入正式标注
- 基于历史任务创建时，系统默认填充历史任务参数，但需求方可以修改
- 基于历史任务创建时，也可以选择保留试标报价流程
- 直接进入正式标注的任务发布后，指定标注方可直接进入正式标注工作台

### 9.5 正式标注提交规则

- 只有被需求方选择的标注方可以进入正式标注工作台
- 直接进入正式标注的任务，只有指定标注方可以进入正式标注工作台
- 标注方首次进入正式标注工作台时设置每包数据条数
- 系统按正式数据顺序自动生成子任务包
- 标注方可以修改每包数据条数
- 修改包大小只影响未提交、未验收的后续包
- 已提交、待验收、已通过或部分驳回的包不重切
- 每条数据必须完成所有标签问题组后才算已标注
- 子任务包内所有数据完成标注后，才允许提交该包
- 提交后子任务包进入「待验收」
- 待验收包不可继续修改，除非被需求方驳回

### 9.6 抽检验收规则

- 需求方以子任务包为单位创建抽检批次
- 抽检批次不能跨多个子任务包
- 同一个子任务包可以创建多个抽检批次
- 抽检方式包括：随机抽检、按标签筛选抽检、手动选择抽检
- 随机抽检使用抽检条数，不使用抽检比例
- 按标签筛选抽检时，需求方可以在多个问题组中选择一个问题组和一个或多个选项进行筛选
- 抽检批次需要记录批次 ID、子任务包 ID、抽检方式、抽检数据 ID、通过数量、驳回数量、创建时间、操作人
- 需求方对抽检数据执行通过或驳回
- 通过的数据进入「已通过」状态
- 驳回的数据必须填写问题类型和驳回原因
- 问题类型固定为：标签错误、图片无法判断、规则理解错误、漏标/少标、其他
- 问题类型为「其他」时，必须填写文本驳回原因
- 只要某条抽检数据被驳回，该子任务包自动进入「部分驳回」状态
- 标注方可以修改被驳回的包，并重新提交整个子任务包
- 重新提交后整个子任务包回到「待验收」，历史抽检和驳回记录保留
- 需求方需要基于重新提交后的包创建新的抽检批次
- 如果包内没有被驳回数据，需求方可以点击「通过整个包」
- 子任务包通过后，包内未抽检数据也视为通过并参与结算
- 所有正式子任务包通过后，任务进入「已完成」

### 9.7 申诉/质疑规则

- 标注方可以针对被驳回的子任务包提交申诉/质疑
- 同一个驳回项只能申诉一次
- 需求方处理申诉/质疑时，可以维持驳回或改为通过
- 需求方处理申诉/质疑时可以追加处理说明
- 如果需求方维持驳回，标注方不能再次申诉，但仍可以修改并重新提交整个子任务包

### 9.8 中止任务规则

- 需求方可以中止未完成任务
- 中止任务时必须填写中止原因
- 中止后任务进入「已中止」状态
- 中止后标注方可以查看任务详情和历史标注结果
- 中止后标注方不能继续标注、提交、报价或申诉
- 中止后已通过验收的数据仍计入预计结算

### 9.9 下载规则

- 下载格式为 `.json`
- 下载文件名建议为 `task-{taskId}-annotations.json`
- 下载内容只包含图像数据和标签结果，不需要包含任务元信息
- 下载结果保留多问题组结构
- 已完成任务可以下载全部正式数据和标签
- 已中止任务可以下载已通过验收的数据和标签
- 待验收、驳回、申诉中的数据不进入下载结果

下载结果结构示例：

```json
[
  {
    "imageUrls": ["https://example.com/image-001.jpg"],
    "labels": [
      { "question": "是否包含违规内容", "value": "否" },
      { "question": "场景类型", "value": ["室内", "人物"] }
    ]
  }
]
```

### 9.10 结算规则

MVP 只展示预计结算，不做真实支付。

试标数据不参与结算。

计算方式：

```text
预计结算金额 = 正式标注通过验收的数据条数 × 需求方选择的报价单价
```

直接进入正式标注且手动填写单价的任务，使用手动单价作为正式结算单价。

已中止任务中，已通过验收的数据仍计入预计结算。

### 9.11 历史任务继承规则

- 发布任务页支持基于历史任务创建新任务
- 历史任务下拉框只展示已完成任务
- 基于历史任务创建时，系统默认填充历史任务参数
- 默认填充的参数均允许修改
- 需求方必须重新确认或修改图片 URL 数据
- 基于历史任务创建后，可以选择直接进入正式标注，也可以选择保留试标报价流程

### 9.12 已发布任务编辑规则

- 已发布但未进入正式标注前，需求方可以编辑任务配置
- 可编辑内容包括任务基础信息、图片 URL 数据、图片展示配置、标签配置、试标配置和指定标注方
- 编辑后不保留编辑历史
- 已提交报价不会自动作废
- 页面需要提示需求方：配置变更后，需自行判断已提交报价是否仍有效

### 9.13 AI 审核工具规则

- 发布任务页展示 AI 审核工具的能力说明
- 后台未来可基于需求方填写的标签选项和判断依据进行校验
- 校验目标包括：选项之间是否存在语义重叠、判断依据是否清晰、是否存在明显缺漏
- MVP 不实现真实后台 AI 审核能力
- MVP 不阻塞任务发布

## 10. 技术约束

### 10.1 技术栈

- Framework：Next.js
- Language：TypeScript
- Styling：可使用 CSS Modules / Tailwind CSS
- Storage：localStorage
- Backend：暂不需要
- Database：暂不需要

### 10.2 localStorage Key 建议

```ts
const STORAGE_KEYS = {
  CURRENT_ROLE: "label_platform_current_role",
  CURRENT_USER_ID: "label_platform_current_user_id",
  DEMO_USERS: "label_platform_demo_users",
  TASKS: "label_platform_tasks",
  CURRENT_REQUESTER_TASK_ID: "label_platform_current_requester_task_id",
};
```

### 10.3 前端路由建议

```text
/
/requester/dashboard
/requester/tasks/new
/annotator/tasks
/annotator/tasks/[taskId]
/annotator/tasks/[taskId]/trial
/annotator/tasks/[taskId]/workspace
/annotator/stats
```

### 10.4 Demo 数据建议

MVP 需要预置 Demo 数据，方便打开页面后直接体验。

模拟用户：

- 1 个需求方
- 3 个标注方

示例任务：

- 示例任务 A：开放试标报价任务，3 个标注方可试标，已有 2 个报价待选择
- 示例任务 B：指定标注方直接正式标注任务，已有一个子任务包待验收
- 额外预置一个已完成历史任务，用于发布任务页「基于历史任务创建」下拉框

示例图片：

- 使用公开网络图片 URL
- 可使用 `https://picsum.photos/...` 作为示例图片来源
- 如果图片加载失败，页面需要展示占位图

## 11. 这一版明确不做什么

MVP 阶段不做以下内容：

1. 不做真实注册、登录、权限系统
2. 不接入数据库
3. 不做服务端 API
4. 不做多人实时协作
5. 不做真实文件上传，图片数据使用 URL 批量录入
6. 不做复杂图片编辑工具
7. 不做框选、语义分割、OCR 等复杂图像标注能力
8. 不做真实后台 AI 审核，只保留能力说明
9. 不做真实支付、发票、合同
10. 不做消息通知系统
11. 不做企业组织、团队管理、角色权限
12. 不做供应商评级体系
13. 不做完整数据导入导出
14. 不做移动端深度适配
15. 不保证 localStorage 数据长期可靠保存

## 12. MVP 验收标准

### 12.1 需求方验收标准

需求方可以完成：

- 进入需求方工作台
- 创建图像标注任务
- 从 0 创建任务
- 基于历史任务创建任务
- 批量录入图片 URL
- 配置图片展示形式
- 配置多个标签问题组、选项、单选或多选、判断依据
- 保存草稿
- 发布任务进入试标阶段
- 发布任务直接进入正式标注阶段
- 指定标注方或开放给多个标注方试标报价
- 查看试标结果和报价
- 查看报价对比表
- 选择报价并确定正式标注方
- 查看任务级状态和子任务包状态
- 针对子任务包创建抽检批次
- 使用随机抽检、按标签筛选抽检或手动选择抽检
- 对抽检数据通过或驳回
- 填写问题类型和驳回原因
- 通过整个子任务包
- 查看并处理标注方申诉/质疑
- 中止任务并填写中止原因
- 查看任务完成状态
- 下载符合规则的 JSON 标注结果

### 12.2 标注方验收标准

标注方可以完成：

- 进入任务大厅
- 查看可参与试标任务
- 查看图片展示配置和标签配置
- 完成试标数据标注
- 提交报价单价和报价说明
- 被选择后进入正式标注工作台
- 设置每包数据条数
- 查看子任务包列表
- 完成子任务包标注
- 保存正式标注进度
- 提交子任务包
- 查看抽检通过或驳回状态
- 查看问题类型和驳回原因
- 修改被驳回子任务包并重新提交整个包
- 针对驳回原因提交一次申诉/质疑
- 查看任务数、子任务包数、数据条数和预计结算

## 13. 推荐首版页面优先级

### 第一阶段：完成主流程

优先实现：

1. 首页身份选择
2. 发布任务页
3. 需求方工作台
4. 标注方任务大厅
5. 试标工作台
6. 正式标注工作台
7. 抽检验收能力
8. 结果下载能力

### 第二阶段：补充体验

继续实现：

1. 标注方统计页
2. 申诉/质疑处理体验
3. 中止任务体验
4. 草稿编辑
5. AI 审核工具说明与前端占位入口

## 14. 产品成功标准

MVP 成功的判断标准：

- 用户可以在同一个浏览器中切换需求方和标注方身份
- 可以创建并保存图像标注任务草稿
- 可以从 0 创建任务，或基于已完成历史任务创建任务
- 可以发布任务并进入试标阶段
- 可以发布任务并直接进入正式标注阶段
- 标注方可以完成试标并提交报价
- 需求方可以选择报价并确定正式标注方
- 正式标注方可以设置包大小并按子任务包完成正式图像标注
- 需求方可以针对子任务包创建抽检批次并完成验收
- 标注方可以基于驳回原因修改整个子任务包或提交一次申诉/质疑
- 需求方可以中止任务
- 需求方可以下载已完成或已中止任务中符合规则的数据
- 任务状态和阶段清晰可见
- 统计中同时展示任务数、子任务包数和数据条数
- 预计结算只基于正式标注通过验收的数据条数计算
- 所有数据刷新页面后仍能通过 localStorage 保留
