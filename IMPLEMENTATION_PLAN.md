# 图像标注平台 MVP 技术实现计划

## 1. 实现目标

基于 `PRD.md` 实现一个可本地演示的图像标注平台 MVP。

技术约束：

- 使用 `Next.js + TypeScript`
- 暂不接入数据库
- 暂不实现服务端 API
- 全部业务数据存储在 `localStorage`
- 支持同一浏览器内切换需求方和标注方身份
- 需要预置 Demo 用户、Demo 任务和公开图片 URL

核心验收目标：

- 需求方可以从 0 创建任务或基于历史任务创建任务
- 需求方可以保存草稿、发布任务、选择试标报价或直接进入正式标注
- 标注方可以试标报价，或直接承接正式标注
- 标注方可以设置子任务包大小并按包提交
- 需求方可以对子任务包创建抽检批次并验收
- 支持部分驳回、一次申诉、重新提交整个包
- 支持中止任务
- 支持按规则下载 `.json` 标注结果

## 2. 推荐实现顺序

### Phase 1：项目基础与数据层

1. 搭建 Next.js + TypeScript 项目结构。
2. 定义核心类型：
   - `Task`
   - `AnnotationItem`
   - `AnnotationPackage`
   - `ReviewBatch`
   - `Quote`
   - `DisplayConfig`
   - `LabelConfig`
   - `DemoUser`
3. 实现 `localStorage` 读写模块：
   - `getTasks`
   - `saveTasks`
   - `getCurrentRole`
   - `setCurrentRole`
   - `getCurrentUser`
   - `setCurrentUser`
   - `resetWithDemoData`
4. 初始化 Demo 数据：
   - 1 个需求方
   - 3 个标注方
   - 1 个开放试标报价任务，已有 2 个报价待选择
   - 1 个指定标注方直接正式标注任务，已有一个子任务包待验收
   - 1 个已完成历史任务，用于「基于历史任务创建」

### Phase 2：首页与身份切换

1. 实现首页 `/`。
2. 提供身份选择：
   - 需求方
   - 标注方
3. 提供 Demo 用户切换：
   - 需求方用户
   - 标注方 A
   - 标注方 B
   - 标注方 C
4. 根据身份跳转：
   - 需求方进入 `/requester/dashboard`
   - 标注方进入 `/annotator/tasks`

### Phase 3：发布任务页

路径：`/requester/tasks/new`

实现内容：

1. 创建方式：
   - 从 0 创建
   - 基于历史任务创建
2. 基于历史任务创建时：
   - 下拉框只展示已完成任务
   - 默认填充历史任务参数
   - 所有字段允许修改
3. 任务进入方式：
   - 试标报价后进入正式标注
   - 直接进入正式标注
4. 表单字段：
   - 任务名称
   - 任务描述
   - 培训说明
   - 标注规则
   - 截止时间
   - 是否指定标注方
   - 指定标注方
   - 手动单价
   - 图片 URL 数据
   - 试标抽取方式
   - 试标数据条数
   - 图片展示配置
   - 多个标签问题组
5. 图片数据解析：
   - 每行一条数据
   - 英文逗号分隔多张图片 URL
6. 试标抽取：
   - 前 N 条
   - 随机 N 条
   - 随机结果在发布时固定保存
7. 表单校验：
   - 从 0 创建并直接进入正式标注时，必须指定标注方
   - 从 0 创建并直接进入正式标注时，必须填写手动单价
   - 试标报价流程必须填写试标数据条数
   - 每个标签问题组至少有 1 个选项
8. 保存草稿：
   - 状态为 `draft`
   - 不进入标注方任务大厅
9. 发布任务：
   - 试标报价流程进入 `pending_trial`
   - 直接正式标注进入 `formal_in_progress`

### Phase 4：需求方工作台

路径：`/requester/dashboard`

页面结构：

1. 顶部统计卡片：
   - 任务数
   - 正式标注中任务数
   - 待验收任务数
   - 已完成任务数
   - 已中止任务数
   - 子任务包数
   - 数据条数
   - 预计结算金额
2. 任务列表：
   - 点击任务后展开或展示详情
   - 展示任务级状态和子任务包状态
3. 任务详情：
   - 基础信息
   - 展示配置
   - 标签配置
   - 报价对比表
   - 子任务包列表
   - 抽检批次
   - 申诉/质疑
   - 下载区域
   - 中止区域

核心操作：

1. 选择报价：
   - 当前报价变为 `selected`
   - 其他报价变为 `not_selected`
   - 任务进入 `formal_in_progress`
   - 设置 `selectedAnnotatorId`
   - 设置 `quotedUnitPrice`
2. 编辑任务：
   - 仅允许已发布但未进入正式标注的任务编辑
   - 不保留编辑历史
   - 页面提示已提交报价不会自动作废
3. 创建抽检批次：
   - 只能针对单个子任务包
   - 支持随机抽检
   - 支持按标签筛选抽检
   - 支持手动选择抽检
4. 验收抽检数据：
   - 可通过抽检数据
   - 可驳回抽检数据
   - 驳回必须填写问题类型和原因
   - 任一抽检数据被驳回，包自动进入 `partially_rejected`
5. 通过整个包：
   - 包内没有被驳回数据时可操作
   - 包状态变为 `approved`
   - 包内未抽检数据也视为通过
6. 处理申诉：
   - 维持驳回
   - 改为通过
   - 可追加处理说明
7. 中止任务：
   - 必须填写中止原因
   - 任务状态变为 `cancelled`
   - 标注方不可继续提交、报价或申诉
8. 下载结果：
   - 已完成任务下载全部正式数据和标签
   - 已中止任务下载已通过验收的数据和标签
   - 文件名：`task-{taskId}-annotations.json`

### Phase 5：标注方任务大厅

路径：`/annotator/tasks`

实现内容：

1. 可试标任务列表。
2. 已参与试标任务列表。
3. 已中标正式任务列表。
4. 任务隐藏规则：
   - 指定给其他标注方的任务不展示
   - 草稿任务不展示
   - 已中止任务只允许查看，不允许操作
5. 操作入口：
   - 查看详情
   - 参与试标
   - 进入试标工作台
   - 进入正式标注工作台

### Phase 6：试标工作台

路径：`/annotator/tasks/[taskId]/trial`

实现内容：

1. 展示任务规则、培训说明、图片展示配置、标签配置。
2. 展示试标数据。
3. 标注方完成所有试标数据。
4. 输入报价单价和报价说明。
5. 提交试标报价后：
   - 新增或更新 `Quote`
   - 报价状态为 `submitted`
   - 任务状态更新为 `pending_quote_selection`

### Phase 7：正式标注工作台

路径：`/annotator/tasks/[taskId]/workspace`

页面结构：

1. 任务信息区。
2. 子任务包设置区。
3. 子任务包列表。
4. 包内标注区。
5. 驳回原因区。
6. 申诉/质疑区。

核心规则：

1. 首次进入正式工作台时，标注方设置每包数据条数。
2. 系统按正式数据顺序自动生成子任务包。
3. 修改包大小只影响未提交、未验收的后续包。
4. 已提交、待验收、已通过、部分驳回的包不重切。
5. 每条数据必须完成所有标签问题组。
6. 子任务包必须整体提交。
7. 部分驳回的包重新提交时，整个包回到 `pending_review`。
8. 重新提交保留历史抽检和驳回记录。
9. 同一个驳回项只能申诉一次。

图片展示规则：

1. 单张图片：每条数据展示一张图片。
2. 多张并列：窗口内全部并列展示，允许横向滚动。
3. 多张切换：展示单张图片，提供左右切换和缩略图列表。
4. 双击放大：
   - 弹窗居中展示
   - 全屏沉浸展示
5. 图片加载失败时展示占位图。

### Phase 8：统计与下载

统计先放在工作台中，不单独实现统计页也可验收 MVP。

需求方统计：

- 任务数
- 各状态任务数
- 子任务包数
- 数据条数
- 通过数据条数
- 驳回数据条数
- 预计结算金额

标注方统计：

- 已参与试标任务数
- 已报价任务数
- 已中标任务数
- 子任务包数
- 已提交包数
- 已通过包数
- 驳回包数
- 已标注数据条数
- 通过数据条数
- 预计结算金额

下载 JSON 示例：

```json
[
  {
    "imageUrls": ["https://example.com/image-001.jpg"],
    "labels": [
      { "question": "是否包含违规内容", "value": "否" },
      { "question": "场景类型", "value": ["室内", "人物"] }
    ]
  }
]
```

## 3. 状态机实现

### 3.1 任务状态

| 当前状态 | 操作 | 下一个状态 |
| --- | --- | --- |
| `draft` | 发布为试标报价任务 | `pending_trial` |
| `draft` | 发布为直接正式标注任务 | `formal_in_progress` |
| `pending_trial` | 标注方开始试标 | `trial_in_progress` |
| `trial_in_progress` | 标注方提交报价 | `pending_quote_selection` |
| `pending_quote_selection` | 需求方选择报价 | `formal_in_progress` |
| `formal_in_progress` | 标注方提交子任务包 | `pending_review` |
| `pending_review` | 任一抽检数据被驳回 | `partially_rejected` |
| `pending_review` | 所有子任务包通过 | `completed` |
| `partially_rejected` | 标注方重新提交整个包 | `pending_review` |
| 任意未完成状态 | 需求方中止任务 | `cancelled` |

### 3.2 子任务包状态

| 当前状态 | 操作 | 下一个状态 |
| --- | --- | --- |
| `not_started` | 标注方进入包内标注 | `in_progress` |
| `in_progress` | 标注方提交包 | `pending_review` |
| `pending_review` | 抽检数据被驳回 | `partially_rejected` |
| `pending_review` | 需求方通过整个包 | `approved` |
| `partially_rejected` | 标注方重新提交整个包 | `pending_review` |

## 4. 关键实现细节

### 4.1 localStorage Key

```ts
const STORAGE_KEYS = {
  CURRENT_ROLE: "label_platform_current_role",
  CURRENT_USER_ID: "label_platform_current_user_id",
  DEMO_USERS: "label_platform_demo_users",
  TASKS: "label_platform_tasks",
  CURRENT_REQUESTER_TASK_ID: "label_platform_current_requester_task_id",
};
```

### 4.2 问题类型枚举

```ts
type RejectionIssueType =
  | "label_error"
  | "image_unclear"
  | "rule_misunderstanding"
  | "missing_annotation"
  | "other";
```

展示文案：

- 标签错误
- 图片无法判断
- 规则理解错误
- 漏标/少标
- 其他

### 4.3 抽检批次字段

每个抽检批次至少保存：

- 批次 ID
- 任务 ID
- 子任务包 ID
- 抽检方式
- 抽检数据 ID
- 通过数量
- 驳回数量
- 创建时间
- 操作人

### 4.4 标签配置

标签配置支持多个问题组。

每个问题组：

- 标题
- 单选或多选
- 多个选项
- 每个选项的判断依据

每条数据必须完成所有问题组后才算已标注。

## 5. 验收测试清单

### 5.1 任务创建

- 可以从 0 创建任务并保存草稿。
- 可以基于已完成历史任务创建任务。
- 可以编辑历史任务带出的默认参数。
- 从 0 创建并直接进入正式标注时，未指定标注方不能发布。
- 从 0 创建并直接进入正式标注时，未填写手动单价不能发布。
- 试标抽取随机 N 条时，发布后抽取结果固定。

### 5.2 试标报价

- 未指定标注方时，多个标注方可以试标报价。
- 指定标注方时，其他标注方看不到任务。
- 报价对比表展示标注方、单价、说明、提交时间和选择按钮。
- 选择一个报价后，其他报价变为未中标且不可修改。

### 5.3 正式标注

- 标注方可以设置每包数据条数。
- 系统按顺序生成子任务包。
- 修改包大小不影响已提交或已验收的包。
- 每条数据必须完成所有问题组后才算完成。
- 子任务包可以整体提交。

### 5.4 抽检验收

- 需求方可以针对单个子任务包创建抽检批次。
- 支持随机抽检、按标签筛选抽检、手动选择抽检。
- 抽检批次记录完整。
- 抽检数据被驳回时必须填写问题类型和原因。
- 任一抽检数据被驳回，包进入部分驳回。
- 包重新提交后回到待验收，并保留历史抽检记录。
- 包通过后，未抽检数据也视为通过并参与结算。

### 5.5 申诉与中止

- 同一个驳回项只能申诉一次。
- 需求方可以维持驳回或改为通过。
- 维持驳回后，标注方不能再次申诉，但可以修改并重新提交包。
- 需求方可以中止任务。
- 中止任务必须填写原因。
- 中止后标注方不能继续标注、提交、报价或申诉。

### 5.6 下载与统计

- 已完成任务可以下载全部正式数据和标签。
- 已中止任务只下载已通过验收的数据和标签。
- 待验收、驳回、申诉中的数据不进入下载结果。
- 下载文件名符合 `task-{taskId}-annotations.json`。
- 统计中展示任务数、子任务包数和数据条数。

## 6. 不做事项

MVP 不实现：

- 真实注册登录
- 真实数据库
- 服务端 API
- 真实图片上传
- 真实 AI 审核
- 真实支付结算
- 多人实时协作
- 复杂图像标注能力，如框选、分割、OCR
- 移动端深度适配
