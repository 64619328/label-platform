# 图像标注平台 MVP 产品验收方案

## 1. 验收目标

本验收方案用于确认图像标注平台 MVP 是否达到可演示、可体验、可跑通完整标注任务周期的标准。

验收重点：

- 需求方可以完成任务创建、发布、报价选择、抽检验收、中止和结果下载
- 标注方可以完成试标报价、正式标注、子任务包提交、驳回修改和一次申诉
- 平台可以通过 `localStorage` 保留数据
- 核心状态流转符合 PRD
- MVP 边界清晰，不要求真实登录、数据库、后端 API、真实上传、真实支付和真实 AI 审核

## 2. 验收环境

### 2.1 技术环境

- Node.js：本机可运行 Next.js 项目
- 应用框架：Next.js + TypeScript
- 数据存储：localStorage
- 浏览器：Chrome 或 Codex in-app browser
- 启动命令：`npm run dev`
- 本地访问地址：`http://localhost:3000`

### 2.2 构建验收

执行：

```bash
npm run build
```

通过标准：

- 构建成功
- TypeScript 无阻断错误
- Next.js 路由正常生成

## 3. Demo 数据要求

### 3.1 用户数据

系统需要预置以下 Demo 用户：

| 用户 | 角色 | 用途 |
| --- | --- | --- |
| 需求方 · 数据团队 | 需求方 | 发布任务、选择报价、验收、下载 |
| 标注方 A | 标注方 | 试标、正式标注、申诉 |
| 标注方 B | 标注方 | 试标报价竞标 |
| 标注方 C | 标注方 | 验证未参与或不可见任务 |

### 3.2 任务数据

系统需要预置以下任务：

| 示例任务 | 状态 | 验收用途 |
| --- | --- | --- |
| 开放试标报价任务 | 待选择报价 | 验证报价对比、选择报价 |
| 指定标注方直接正式标注任务 | 待验收 | 验证子任务包、抽检验收 |
| 已完成历史任务 | 已完成 | 验证基于历史任务创建 |

### 3.3 图片数据

通过标准：

- 示例图片使用公开网络图片 URL
- 图片可正常展示
- 图片加载失败时展示占位状态

## 4. 验收范围

### 4.1 本次验收范围内

- 首页身份切换
- Demo 用户切换
- localStorage 数据初始化和保留
- 需求方工作台
- 发布任务页
- 标注方任务大厅
- 标注方任务详情页
- 试标工作台
- 正式标注工作台
- 报价对比和报价选择
- 子任务包生成、提交、验收
- 抽检批次创建
- 随机抽检、按标签筛选抽检、手动选择抽检
- 抽检数据通过和驳回
- 驳回问题类型和原因填写
- 标注方一次申诉
- 需求方处理申诉
- 中止任务
- JSON 结果下载
- 统计卡片展示

### 4.2 本次验收范围外

- 真实注册登录
- 真实权限系统
- 真实数据库
- 服务端 API
- 真实文件上传
- 真实 AI 审核
- 真实支付结算
- 多人实时协作
- 框选、分割、OCR 等复杂图像标注工具
- 移动端深度适配

## 5. 主流程验收用例

## 5.1 首页与身份切换

### 用例 A01：首页展示

操作步骤：

1. 打开 `http://localhost:3000`
2. 查看首页内容

预期结果：

- 展示产品名称
- 展示 MVP 说明
- 展示需求方和标注方入口
- 展示 Demo 用户选择
- 支持进入需求方工作台
- 支持进入标注方任务大厅

验收结论：通过 / 不通过

### 用例 A02：切换需求方身份

操作步骤：

1. 在首页选择「需求方 · 数据团队」
2. 进入需求方工作台

预期结果：

- 当前身份显示为需求方
- 页面进入 `/requester/dashboard`
- 可以看到需求方任务列表和统计卡片

验收结论：通过 / 不通过

### 用例 A03：切换标注方身份

操作步骤：

1. 在首页选择「标注方 A」
2. 进入标注方任务大厅

预期结果：

- 当前身份显示为标注方 A
- 页面进入 `/annotator/tasks`
- 可以看到标注方可见任务

验收结论：通过 / 不通过

## 5.2 需求方创建任务

### 用例 B01：从 0 创建并保存草稿

操作步骤：

1. 以需求方身份进入发布任务页
2. 选择「从 0 创建」
3. 填写任务名称、描述、培训说明、标注规则
4. 填写图片 URL 数据
5. 配置图片展示方式
6. 配置标签问题组
7. 点击「保存草稿」

预期结果：

- 任务保存为草稿
- 草稿不进入标注方任务大厅
- 刷新页面后草稿仍保留在 localStorage 中

验收结论：通过 / 不通过

### 用例 B02：发布试标报价任务

操作步骤：

1. 以需求方身份进入发布任务页
2. 选择「从 0 创建」
3. 选择「试标报价后进入正式标注」
4. 配置试标抽取方式为「前 N 条」或「随机 N 条」
5. 填写试标条数
6. 点击「发布任务」

预期结果：

- 任务进入试标阶段
- 状态为待试标或试标相关状态
- 试标数据和正式数据被正确拆分
- 随机抽取结果在发布后固定
- 标注方任务大厅可看到该任务

验收结论：通过 / 不通过

### 用例 B03：直接进入正式标注

操作步骤：

1. 以需求方身份进入发布任务页
2. 选择「从 0 创建」
3. 选择「直接进入正式标注」
4. 指定标注方
5. 填写手动单价
6. 点击「发布任务」

预期结果：

- 未指定标注方时不允许发布
- 未填写手动单价时不允许发布
- 发布成功后任务进入正式标注阶段
- 只有指定标注方可以看到并进入正式标注工作台

验收结论：通过 / 不通过

### 用例 B04：基于历史任务创建

操作步骤：

1. 以需求方身份进入发布任务页
2. 选择「基于历史任务创建」
3. 选择一个已完成历史任务
4. 查看表单字段
5. 修改部分配置
6. 发布任务

预期结果：

- 历史任务下拉框只展示已完成任务
- 系统默认填充历史任务参数
- 默认填充的参数均可修改
- 新任务发布后与历史任务相互独立

验收结论：通过 / 不通过

## 5.3 试标报价与报价选择

### 用例 C01：标注方提交试标报价

操作步骤：

1. 切换为标注方 A
2. 进入可试标任务
3. 打开试标工作台
4. 对所有试标数据完成所有问题组标注
5. 填写每条数据报价单价
6. 填写报价说明
7. 提交试标和报价

预期结果：

- 未完成所有试标数据时不能提交报价
- 未填写报价单价时不能提交报价
- 提交后生成报价记录
- 任务状态进入待选择报价
- 需求方可在报价对比表中看到该报价

验收结论：通过 / 不通过

### 用例 C02：需求方选择报价

操作步骤：

1. 切换为需求方
2. 进入需求方工作台
3. 选择开放试标报价任务
4. 查看报价对比表
5. 点击某个报价的「选择」

预期结果：

- 被选择报价状态变为 selected
- 其他报价状态变为 not_selected
- 其他报价不可再修改
- 任务进入正式标注阶段
- 系统记录正式标注方
- 系统记录正式结算单价

验收结论：通过 / 不通过

## 5.4 正式标注与子任务包

### 用例 D01：生成子任务包

操作步骤：

1. 切换为中标或指定标注方
2. 进入正式标注工作台
3. 设置每包数据条数
4. 点击生成或调整子任务包

预期结果：

- 系统按正式数据顺序自动生成子任务包
- 最后一个包可少于每包数据条数
- 包列表展示包状态和数据条数
- 修改包大小不影响已提交、已验收、已通过或部分驳回的包

验收结论：通过 / 不通过

### 用例 D02：提交子任务包

操作步骤：

1. 标注方进入某个子任务包
2. 对包内每条数据完成所有问题组标注
3. 点击提交当前子任务包

预期结果：

- 未完成所有问题组时不能提交
- 子任务包作为整体提交
- 提交后子任务包进入待验收
- 提交后需求方可以创建抽检批次

验收结论：通过 / 不通过

## 5.5 抽检验收

### 用例 E01：随机抽检

操作步骤：

1. 切换为需求方
2. 进入待验收任务
3. 选择一个待验收子任务包
4. 选择抽检方式为随机抽检
5. 填写抽检条数
6. 创建抽检批次

预期结果：

- 抽检批次创建成功
- 抽检批次记录批次 ID、子任务包 ID、抽检方式、抽检数据 ID、通过数量、驳回数量、创建时间、操作人
- 抽检数据展示在验收区域

验收结论：通过 / 不通过

### 用例 E02：按标签筛选抽检

操作步骤：

1. 在需求方工作台选择待验收子任务包
2. 选择抽检方式为按标签筛选
3. 选择一个问题组
4. 选择一个或多个标签选项
5. 创建抽检批次

预期结果：

- 系统基于标注结果筛选数据
- 抽检批次只包含符合标签条件的数据
- 抽检批次记录完整

验收结论：通过 / 不通过

### 用例 E03：手动选择抽检

操作步骤：

1. 在需求方工作台选择待验收子任务包
2. 选择抽检方式为手动选择
3. 勾选包内若干数据
4. 创建抽检批次

预期结果：

- 抽检批次只包含手动选择的数据
- 抽检批次不能跨多个子任务包

验收结论：通过 / 不通过

### 用例 E04：通过整个子任务包

操作步骤：

1. 创建抽检批次
2. 对抽检数据点击通过
3. 点击通过整个包

预期结果：

- 子任务包状态变为已通过
- 包内未抽检数据也视为通过
- 通过数据参与预计结算

验收结论：通过 / 不通过

### 用例 E05：驳回抽检数据

操作步骤：

1. 创建抽检批次
2. 选择某条抽检数据
3. 选择问题类型
4. 填写驳回原因
5. 点击驳回

预期结果：

- 未填写问题类型或驳回原因时不能驳回
- 抽检数据状态变为驳回
- 子任务包自动进入部分驳回
- 任务状态进入部分驳回
- 标注方可看到问题类型和驳回原因

验收结论：通过 / 不通过

## 5.6 驳回修改与申诉

### 用例 F01：标注方修改并重新提交整个包

操作步骤：

1. 切换为标注方
2. 进入部分驳回任务包
3. 查看问题类型和驳回原因
4. 修改包内数据标注
5. 重新提交整个子任务包

预期结果：

- 只能重新提交整个子任务包
- 重新提交后子任务包回到待验收
- 历史抽检和驳回记录保留
- 需求方需要重新创建抽检批次

验收结论：通过 / 不通过

### 用例 F02：标注方提交一次申诉

操作步骤：

1. 标注方进入被驳回数据
2. 填写申诉或质疑原因
3. 提交申诉
4. 尝试再次申诉同一驳回项

预期结果：

- 第一次申诉成功
- 同一驳回项不能再次申诉
- 需求方工作台展示申诉内容

验收结论：通过 / 不通过

### 用例 F03：需求方处理申诉

操作步骤：

1. 切换为需求方
2. 进入申诉区域
3. 选择维持驳回或改为通过

预期结果：

- 维持驳回后，标注方不能再次申诉，但可以修改并重新提交包
- 改为通过后，对应数据进入通过状态
- 处理说明可展示或保存

验收结论：通过 / 不通过

## 5.7 中止任务

### 用例 G01：需求方中止任务

操作步骤：

1. 需求方进入未完成任务
2. 输入中止原因
3. 点击中止任务

预期结果：

- 未填写中止原因时不能中止
- 任务状态变为已中止
- 标注方可查看任务详情和历史标注结果
- 标注方不能继续标注、提交、报价或申诉
- 已通过验收的数据仍计入预计结算

验收结论：通过 / 不通过

## 5.8 结果下载

### 用例 H01：已完成任务下载

操作步骤：

1. 将任务所有正式子任务包验收通过
2. 点击下载 JSON

预期结果：

- 下载文件名符合 `task-{taskId}-annotations.json`
- 下载内容包含全部正式数据和标签
- 下载内容保留多问题组结构
- 下载内容不包含任务元信息

验收结论：通过 / 不通过

### 用例 H02：已中止任务下载

操作步骤：

1. 中止一个已有部分通过数据的任务
2. 点击下载 JSON

预期结果：

- 下载内容只包含已通过验收的数据和标签
- 待验收、驳回、申诉中的数据不进入下载结果

验收结论：通过 / 不通过

## 5.9 图片展示

### 用例 I01：多图并列展示

操作步骤：

1. 打开多图并列展示任务
2. 查看一条包含多张图片的数据

预期结果：

- 同一条数据下多张图片在窗口内并列展示
- 图片过多时允许横向滚动

验收结论：通过 / 不通过

### 用例 I02：多图切换展示

操作步骤：

1. 打开多图切换展示任务
2. 查看一条包含多张图片的数据
3. 点击上一张、下一张或缩略图

预期结果：

- 同一条数据只展示一张主图
- 支持左右切换
- 支持缩略图选择

验收结论：通过 / 不通过

### 用例 I03：双击放大

操作步骤：

1. 打开支持双击放大的任务
2. 双击图片

预期结果：

- 图片按配置弹窗居中展示或全屏沉浸展示
- 可以关闭放大预览

验收结论：通过 / 不通过

### 用例 I04：图片加载失败

操作步骤：

1. 使用一个无效图片 URL 创建任务或查看包含无效 URL 的数据

预期结果：

- 图片加载失败时展示占位状态
- 页面不崩溃

验收结论：通过 / 不通过

## 6. 状态流转验收

### 6.1 任务状态流转

| 起始状态 | 操作 | 预期状态 |
| --- | --- | --- |
| 草稿 | 发布为试标报价任务 | 待试标 |
| 草稿 | 发布为直接正式标注任务 | 正式标注中 |
| 待试标 | 标注方开始或提交试标 | 试标中 / 待选择报价 |
| 待选择报价 | 需求方选择报价 | 正式标注中 |
| 正式标注中 | 标注方提交子任务包 | 待验收 |
| 待验收 | 抽检数据被驳回 | 部分驳回 |
| 待验收 | 所有子任务包通过 | 已完成 |
| 部分驳回 | 标注方重新提交整个包 | 待验收 |
| 任意未完成状态 | 需求方中止任务 | 已中止 |

通过标准：

- 状态变化与表格一致
- 不出现无法解释的中间状态
- 状态对应的可操作按钮正确显示或禁用

### 6.2 子任务包状态流转

| 起始状态 | 操作 | 预期状态 |
| --- | --- | --- |
| 未开始 | 标注方进入包内标注 | 标注中 |
| 标注中 | 标注方提交包 | 待验收 |
| 待验收 | 抽检数据被驳回 | 部分驳回 |
| 待验收 | 需求方通过整个包 | 已通过 |
| 部分驳回 | 标注方重新提交整个包 | 待验收 |

通过标准：

- 子任务包状态变化正确
- 已提交、待验收、已通过或部分驳回的包不因修改包大小被重切

## 7. 表单与校验验收

| 场景 | 校验规则 | 预期结果 |
| --- | --- | --- |
| 从 0 创建并直接正式标注 | 必须指定标注方 | 未指定时不允许发布 |
| 从 0 创建并直接正式标注 | 必须填写手动单价 | 未填写时不允许发布 |
| 试标报价流程 | 必须填写试标条数 | 未填写时不允许发布 |
| 标签配置 | 每个问题组至少有 1 个选项 | 不满足时不允许发布 |
| 试标提交 | 所有试标数据必须完成所有问题组 | 不满足时不能提交报价 |
| 子任务包提交 | 包内所有数据必须完成所有问题组 | 不满足时不能提交包 |
| 驳回数据 | 必须填写问题类型和驳回原因 | 不满足时不能驳回 |
| 中止任务 | 必须填写中止原因 | 不满足时不能中止 |

## 8. 统计验收

需求方工作台需展示并正确更新：

- 任务数
- 正式标注中任务数
- 待验收任务数
- 已完成任务数
- 已中止任务数
- 子任务包数
- 正式数据条数
- 已通过数据条数
- 预计结算金额

标注方任务大厅或工作台需展示并正确更新：

- 可试标任务数
- 已中标或指定任务数
- 我的子任务包数
- 预计结算金额

预计结算计算规则：

```text
预计结算金额 = 正式标注通过验收的数据条数 × 正式结算单价
```

## 9. localStorage 验收

操作步骤：

1. 创建或修改一个任务
2. 切换身份
3. 刷新页面
4. 重新进入对应页面

预期结果：

- 当前身份保留
- Demo 用户保留
- 任务数据保留
- 报价、标注、抽检、驳回、中止、下载相关状态保留

## 10. 非功能验收

### 10.1 易用性

通过标准：

- 核心入口清晰
- 需求方和标注方视角可区分
- 任务状态、子任务包状态、抽检状态可识别
- 关键操作有明确按钮
- 禁用状态不会误导用户

### 10.2 稳定性

通过标准：

- 页面刷新后不丢失 localStorage 数据
- 图片加载失败不导致页面崩溃
- 空任务、空报价、空子任务包、空抽检批次有空状态
- 不因角色切换导致页面崩溃

### 10.3 响应式

通过标准：

- 桌面端主要页面布局正常
- 小屏下页面可以纵向浏览
- 表单、表格和图片区域不出现严重遮挡

## 11. 验收通过标准

MVP 产品验收通过需同时满足：

1. `npm run build` 通过
2. 首页、需求方工作台、发布任务页、标注方任务大厅、试标工作台、正式标注工作台均可访问
3. 至少完整跑通 1 条试标报价任务链路
4. 至少完整跑通 1 条直接正式标注任务链路
5. 至少完成 1 次子任务包提交
6. 至少完成 1 次抽检批次创建
7. 至少完成 1 次抽检通过并通过整个包
8. 至少完成 1 次抽检驳回、标注方修改或申诉、需求方处理
9. 至少完成 1 次任务中止
10. 至少完成 1 次 JSON 下载
11. localStorage 刷新保留数据
12. 无阻断性页面崩溃或核心流程断点

## 12. 验收记录模板

| 验收项 | 验收人 | 日期 | 结果 | 问题记录 |
| --- | --- | --- | --- | --- |
| 首页与身份切换 |  |  | 通过 / 不通过 |  |
| 任务创建与草稿 |  |  | 通过 / 不通过 |  |
| 试标报价 |  |  | 通过 / 不通过 |  |
| 报价选择 |  |  | 通过 / 不通过 |  |
| 正式标注 |  |  | 通过 / 不通过 |  |
| 子任务包提交 |  |  | 通过 / 不通过 |  |
| 抽检验收 |  |  | 通过 / 不通过 |  |
| 驳回与申诉 |  |  | 通过 / 不通过 |  |
| 中止任务 |  |  | 通过 / 不通过 |  |
| JSON 下载 |  |  | 通过 / 不通过 |  |
| localStorage 保留 |  |  | 通过 / 不通过 |  |
| 构建验证 |  |  | 通过 / 不通过 |  |

## 13. 风险与备注

- 当前 MVP 使用 localStorage，数据仅适用于本地演示，不保证长期可靠保存
- 当前 MVP 不做真实登录和权限隔离，身份切换仅用于演示
- 当前 MVP 不做真实图片上传，图片依赖外部 URL 可访问性
- 当前 MVP 不实现真实 AI 审核，仅保留能力说明
- 当前 MVP 不做真实支付结算，金额仅为预计结算展示
